# ============================================================
# 交易規則 —— 這份檔案就是「寫死在系統裡的紀律」
# ============================================================
# 改這裡的數字 = 改變你的交易規則。
# 每次修改請在 changelog 留一行，兩個月後覆盤時你會需要它。
# ------------------------------------------------------------

# 你在最佳化什麼？寫成一句可量測的話。
# 這行不影響程式運作，但每天都會印在報告最上面，提醒你自己。
objective: >-
  未來 12 個月產生可支配現金流，同時整體最大回撤不超過 15%。
  （這是預設範例，請改成你自己的目標）

rules:
  # 停損 / 停利閾值（百分比）
  stop_loss_pct: 10.0      # 跌破成本 10% → 觸發停損訊號
  take_profit_pct: 22.0    # 漲超成本 22% → 觸發停利訊號

  # 停損基準:
  #   cost     = 以成本價計算（傳統固定停損）
  #   trailing = 以「進場後最高價」計算（移動停損，會隨獲利往上調）
  # trailing 需要歷史高點，資料來自 data/signals.jsonl，
  # 剛開始跑的前幾天會退化成 max(成本, 現價)，累積越久越準確。
  stop_basis: cost

  # 「接近」的定義：距離閾值幾個百分點以內就先預警
  near_threshold_pct: 3.0

# ------------------------------------------------------------
# 個股例外規則（可選）
# 高波動個股用同一組固定 % 容易被雜訊掃出場，可以在這裡單獨放寬。
# 未列出的個股一律套用上面的 rules。
# ------------------------------------------------------------
overrides:
  # "2330":
  #   stop_loss_pct: 15.0
  #   take_profit_pct: 30.0

# ------------------------------------------------------------
# 尚未實作（Phase 1.5）
# ------------------------------------------------------------
# mode: atr —— 以 N 倍 ATR 取代固定百分比，讓停損寬度隨個股波動度調整。
# 需要每檔的日 K 歷史（TWSE STOCK_DAY 逐檔月報表），目前 Phase 1 尚未接。
# 設計上 rules 已預留 mode 欄位，接上歷史資料後即可切換。

changelog:
  - date: "2026-07-29"
    note: "初版建立：10% 停損 / 22% 停利，以成本價為基準。"
